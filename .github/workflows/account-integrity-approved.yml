name: Apply approved account integrity patch

on:
  push:
    paths:
      - ".github/workflows/account-integrity-approved.yml"

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v5
        with:
          node-version: 24
      - name: Apply approved account integrity changes
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          def read(path):
              return Path(path).read_text()

          def write(path, text):
              Path(path).write_text(text)

          def replace_once(path, old, new):
              text = read(path)
              if old not in text:
                  raise SystemExit(f"Expected source not found in {path}: {old[:120]!r}")
              if text.count(old) != 1:
                  raise SystemExit(f"Expected one source match in {path}, found {text.count(old)}")
              write(path, text.replace(old, new, 1))

          def replace_block(path, start, end, replacement):
              text = read(path)
              first = text.find(start)
              if first < 0:
                  raise SystemExit(f"Block start not found in {path}: {start}")
              last = text.find(end, first)
              if last < 0:
                  raise SystemExit(f"Block end not found in {path}: {end}")
              write(path, text[:first] + replacement + text[last:])

          ledger_helpers = '''  let accountSpendSubmitPending = false;

  function accountMutationCanWrite() {
    const architecture = window.FinanceProfileArchitecture;
    if (!architecture || architecture.canWrite?.() !== false) return true;
    showToast("This Viewer profile is read-only", "warning");
    return false;
  }

  function expectedBalancesMatch(expectedBalances = []) {
    return expectedBalances.every(item => Object.prototype.hasOwnProperty.call(data.accounts || {}, item.account)
      && roundMoney(data.accounts[item.account]) === roundMoney(item.target));
  }

  function profileBalancesMatch(expectedBalances = []) {
    if (!expectedBalances.length) return true;
    const architecture = window.FinanceProfileArchitecture;
    const profileId = architecture?.activeProfileId?.();
    if (!profileId) return true;
    try {
      const raw = localStorage.getItem(`simple-finance-profile-data-v1:${profileId}`);
      if (!raw) return true;
      const stored = JSON.parse(raw);
      return expectedBalances.every(item => Object.prototype.hasOwnProperty.call(stored.accounts || {}, item.account)
        && roundMoney(stored.accounts[item.account]) === roundMoney(item.target));
    } catch (error) {
      console.error("Could not verify profile-scoped account persistence.", error);
      return false;
    }
  }

  function restoreAccountMutation(snapshot, message = "Account changes were not saved") {
    data = normalizeData(cloneData(snapshot));
    try {
      if (typeof persistFinanceDataRaw === "function") persistFinanceDataRaw("Account correction rolled back");
      else localStorage.setItem(STORAGE_KEY, JSON.stringify(data));
    } catch (error) {
      console.error("Could not persist the account rollback; the prior stored copy remains authoritative.", error);
    }
    try { renderAll(false); } catch (error) { console.error("Could not refresh account state after rollback.", error); }
    showToast(message, "warning");
    return false;
  }

  function persistAccountMutation(snapshot, message, expectedBalances = []) {
    recalculateBalances(data);
    if (!expectedBalancesMatch(expectedBalances)) {
      return restoreAccountMutation(snapshot, "The corrected account balance could not be verified. Nothing was saved.");
    }
    try {
      const saved = saveData(message);
      if (saved === false) return restoreAccountMutation(snapshot, "Account changes were not saved. Check profile permissions and try again.");
    } catch (error) {
      console.error("Account changes could not be persisted.", error);
      return restoreAccountMutation(snapshot, "Account changes could not be saved on this device.");
    }
    if (!expectedBalancesMatch(expectedBalances)) {
      return restoreAccountMutation(snapshot, "The account balance changed during save. The previous value was restored.");
    }
    if (!profileBalancesMatch(expectedBalances)) {
      const architecture = window.FinanceProfileArchitecture;
      let repaired = false;
      try { repaired = architecture?.persistCurrentData?.(data, message) !== false; } catch (error) { console.error("Could not repair profile-scoped account persistence.", error); }
      if (!repaired || !profileBalancesMatch(expectedBalances)) {
        return restoreAccountMutation(snapshot, "The account update could not be stored in the active profile.");
      }
    }
    return true;
  }

  function setAccountSpendStatus'''
          replace_once(
              "assets/js/account-ledger.js",
              '  let accountSpendSubmitPending = false;\n\n  function setAccountSpendStatus',
              ledger_helpers,
          )

          submit_account = '''  function submitAccountForm() {
    if (!accountMutationCanWrite()) return false;
    const originalName = document.getElementById("originalAccountName").value;
    const newName = document.getElementById("accountName").value.trim();
    if (!validateMoneyInput("accountBalance", { required:false, min:0, message:"Enter a valid account balance of zero or more." })) return false;
    const balanceInput = document.getElementById("accountBalance").value.trim();
    const targetBalance = balanceInput === "" ? 0 : moneyInputValue("accountBalance");
    const type = ACCOUNT_TYPES.includes(document.getElementById("accountType").value) ? document.getElementById("accountType").value : "Other";
    if (!newName || !Number.isFinite(targetBalance)) { showToast("Enter a valid account name and balance", "warning"); return false; }
    const duplicate = accountNames().some(name => name.toLowerCase() === newName.toLowerCase() && name !== originalName);
    if (duplicate) { showToast("An account with this name already exists", "warning"); return false; }

    const snapshot = cloneData(data);
    pushUndo(originalName ? `Edit account ${originalName}` : `Add account ${newName}`);
    const existingOrder = accountNames();
    if (originalName && originalName !== newName) {
      renameAccountReferences(originalName, newName);
      data.accountOrder = existingOrder.map(name => name === originalName ? newName : name);
      data.accounts[newName] = Number(data.accounts[originalName] || 0);
      delete data.accounts[originalName];
      data.accountTypes[newName] = data.accountTypes[originalName];
      delete data.accountTypes[originalName];
      if (data.accountIcons?.[originalName]) { data.accountIcons[newName] = data.accountIcons[originalName]; delete data.accountIcons[originalName]; }
      if (data.savingsSettings?.defaultAccount === originalName) data.savingsSettings.defaultAccount = newName;
    }
    data.accounts[newName] = Number(data.accounts[newName] || 0);
    data.accountTypes[newName] = type;
    data.accountIcons = data.accountIcons || {};
    const accountIcon = pickerIcon("account");
    if (accountIcon) data.accountIcons[newName] = accountIcon; else delete data.accountIcons[newName];
    if (!originalName && !data.accountOrder.includes(newName)) data.accountOrder.push(newName);
    if (!originalName) {
      const openingId = `opening:${uid()}`;
      appendLedgerEntries([{ id:uid(), transactionId:openingId, operationId:openingId, account:newName, type:"opening-balance", amount:targetBalance, date:localDateKey(), description:`Opening balance for ${newName}`, source:"account-create" }]);
    } else appendReconciliation(newName, targetBalance, { note:"Balance changed from Edit account" });
    recalculateBalances(data, { stamp:true });
    if (type !== "Savings") (data.savingsGoals || []).forEach(goal => { if (goal.sourceType === "linked" && goal.linkedAccount === newName) { goal.sourceType = "manual"; goal.currentAmount = Number(data.accounts[newName] || 0); goal.linkedAccount = ""; goal.updatedAt = new Date().toISOString(); } });
    if (type === "Savings" && !data.savingsSettings.defaultAccount) data.savingsSettings.defaultAccount = newName;
    if (type !== "Savings" && data.savingsSettings.defaultAccount === newName) data.savingsSettings.defaultAccount = "";

    const saved = persistAccountMutation(
      snapshot,
      originalName ? "Account updated and reconciled" : "Account added with opening balance",
      [{ account:newName, target:targetBalance }]
    );
    if (!saved) return false;
    closeTrackedFormAfterAction("accountDialog");
    refreshReconciledAccountState(newName, targetBalance);
    return true;
  }

'''
          replace_block(
              "assets/js/account-ledger.js",
              "  function submitAccountForm() {",
              "  function submitAccountsReconciliationForm() {",
              submit_account,
          )

          submit_accounts = '''  function submitAccountsReconciliationForm() {
    if (!accountMutationCanWrite()) return false;
    const accountInputs = [...document.querySelectorAll(".account-input")];
    for (const input of accountInputs) if (!validateMoneyInput(input, { required:false, min:0, message:"Enter a balance of zero or more." })) return false;
    const changes = accountInputs.map(input => ({ account:input.dataset.account, target:moneyInputValue(input) })).filter(item => roundMoney(data.accounts[item.account]) !== roundMoney(item.target));
    const typeChanges = [...document.querySelectorAll(".account-type-input")].filter(select => accountType(select.dataset.accountType) !== select.value);
    if (!changes.length && !typeChanges.length) { showToast("Account balances already match the entered values", "info"); return false; }

    const snapshot = cloneData(data);
    pushUndo("Reconcile account balances");
    typeChanges.forEach(select => { data.accountTypes[select.dataset.accountType] = ACCOUNT_TYPES.includes(select.value) ? select.value : "Other"; });
    changes.forEach(item => appendReconciliation(item.account, item.target, { note:"Balances form reconciliation" }));
    recalculateBalances(data, { stamp:changes.length > 0 });
    (data.savingsGoals || []).forEach(goal => { if (goal.sourceType === "linked" && accountType(goal.linkedAccount) !== "Savings") { goal.currentAmount = Number(data.accounts[goal.linkedAccount] || goal.currentAmount || 0); goal.sourceType = "manual"; goal.linkedAccount = ""; goal.updatedAt = new Date().toISOString(); } });
    if (data.savingsSettings.defaultAccount && accountType(data.savingsSettings.defaultAccount) !== "Savings") data.savingsSettings.defaultAccount = "";

    return persistAccountMutation(
      snapshot,
      `${changes.length} account balance${changes.length === 1 ? "" : "s"} reconciled`,
      changes.map(item => ({ account:item.account, target:item.target }))
    );
  }

'''
          replace_block(
              "assets/js/account-ledger.js",
              "  function submitAccountsReconciliationForm() {",
              "  function submitIncomeForm() {",
              submit_accounts,
          )
          replace_once(
              "assets/js/account-ledger.js",
              '    capabilities:{ accountSpending:true, verifiedSpendSubmit:true, persistentSpendActions:true, transactionalSpend:true, isolatedSpendAction:true },',
              '    capabilities:{ accountSpending:true, verifiedSpendSubmit:true, persistentSpendActions:true, transactionalSpend:true, isolatedSpendAction:true, accountReconciliationOwner:true, transactionalAccountCorrection:true, profileVerifiedAccountCorrection:true },',
          )
          replace_once(
              "assets/js/account-ledger.js",
              '    appendReconciliation,\n    openSpend:openAccountSpendDialog,',
              '    appendReconciliation,\n    submitAccountForm,\n    submitAccountsReconciliationForm,\n    openSpend:openAccountSpendDialog,',
          )

          account_submit_compat = '''"use strict";

/*
 * Account correction submit compatibility and ledger-ownership guard.
 *
 * iOS Safari / installed PWAs can be unreliable when a button changes between
 * type="button" and type="submit" while the account dialog switches between
 * Record spending and Correct account balance. Intercept the correction-mode
 * tap and dispatch the form submit event explicitly so the ledger-aware
 * account handler always runs. Ledger-backed profiles are also blocked from
 * falling through to the legacy direct-balance handler if the ledger owner is
 * stale or unavailable.
 */
(function installAccountSubmitCompat(root) {
  if (!root?.document || root.FinanceAccountSubmitCompat?.installed) return;

  function activeLedgerVersion() {
    try { return typeof data !== "undefined" ? Number(data?.ledgerSettings?.version || 0) : 0; }
    catch (error) { return 0; }
  }

  function ledgerOwnerReady() {
    return Boolean(root.FinanceAccountLedger?.capabilities?.accountReconciliationOwner);
  }

  function guardLedgerBackedAccountSubmit(event) {
    if (!["accountForm", "accountsForm"].includes(event.target?.id)) return;
    if (!activeLedgerVersion() || ledgerOwnerReady()) return;
    event.preventDefault();
    event.stopImmediatePropagation();
    if (typeof root.showToast === "function") root.showToast("Account ledger is updating. Reload Talaan before changing account balances.", "warning");
    else console.warn("Account ledger owner is unavailable; balance edit blocked.");
  }

  function submitCorrectionFromPrimaryAction(event) {
    const primary = event.target?.closest?.("#accountPrimaryAction");
    if (!primary) return;

    const dialog = root.document.getElementById("accountDialog");
    if (!dialog || dialog.dataset.accountMode === "spend") return;

    const form = root.document.getElementById("accountForm");
    if (!form || !form.contains(primary)) return;

    event.preventDefault();
    form.dispatchEvent(new root.Event("submit", { bubbles:true, cancelable:true }));
  }

  root.document.addEventListener("submit", guardLedgerBackedAccountSubmit, true);
  root.document.addEventListener("click", submitCorrectionFromPrimaryAction, true);
  root.FinanceAccountSubmitCompat = Object.freeze({ installed:true, ledgerGuard:true });
})(typeof window !== "undefined" ? window : globalThis);
'''
          write("assets/js/account-submit-compat.js", account_submit_compat)

          replace_once(
              "assets/js/pwa-update.js",
              '  const INCOME_PLANNING_REFRESH_KEY = "finance-income-planning-v2-5-0-talaan1";',
              '  const INCOME_PLANNING_REFRESH_KEY = "finance-income-planning-v2-5-0-talaan1";\n  const ACCOUNT_INTEGRITY_REFRESH_KEY = "finance-account-integrity-v2-5-0-talaan1";',
          )
          replace_once(
              "assets/js/pwa-update.js",
              '      await import("./account-submit-compat.js?v=2.5.0-account-submit1");',
              '      await import("./account-submit-compat.js?v=2.5.0-account-integrity1");',
          )
          refresh_account_integrity = '''
  async function refreshAccountIntegrityRuntimeOnce() {
    if (!root.navigator?.serviceWorker?.controller || !("caches" in root)) return false;
    try {
      if (root.localStorage?.getItem(ACCOUNT_INTEGRITY_REFRESH_KEY) === "done") return false;
      const removed = await deleteCachedPaths([
        "/account-ledger.js",
        "/account-submit-compat.js",
        "/cloud-sync.js",
        "/cloud-sync-lifecycle.js"
      ]);
      if (!removed) return false;
      root.localStorage?.setItem(ACCOUNT_INTEGRITY_REFRESH_KEY, "done");
      root.location.reload();
      return true;
    } catch (error) {
      return false;
    }
  }

'''
          replace_once("assets/js/pwa-update.js", "  const api = {", refresh_account_integrity + "  const api = {")
          replace_once(
              "assets/js/pwa-update.js",
              '  void installBrowserBrandIcons();\n  void installAccountSubmitCompat();\n  void refreshCachedHeaderToolsOnce();',
              '  void installBrowserBrandIcons();\n  void refreshAccountIntegrityRuntimeOnce().then(refreshed => {\n    if (!refreshed) void installAccountSubmitCompat();\n  });\n  void refreshCachedHeaderToolsOnce();',
          )

          replace_once(
              "scripts/prepare-runtime.mjs",
              'const UI_RADIUS_ASSET_QUERY = "2.5.0-talaan4";',
              'const UI_RADIUS_ASSET_QUERY = "2.5.0-talaan4";\nconst ACCOUNT_INTEGRITY_ASSET_QUERY = "2.5.0-account-integrity1";',
          )
          replace_once(
              "scripts/prepare-runtime.mjs",
              '    "account-ledger.js",\n    "brand-icons.js",',
              '    "account-ledger.js",\n    "account-submit-compat.js",\n    "brand-icons.js",',
          )
          replace_once(
              "scripts/prepare-runtime.mjs",
              '    "Includes encrypted multi-profile Cloud Schema V3 synchronization, offline PWA support, recovery safeguards, and five-minute routine sync.",\n    "Preserves Finance Schema 12, Cloud Schema V3, account balances, paid state, recurrence, project payments, backups, encryption, persistent storage identifiers, and synchronization behavior."',
              '    "Includes encrypted multi-profile Cloud Schema V3 synchronization, offline PWA support, recovery safeguards, and five-minute routine sync.",\n    "Hardens Account Ledger balance corrections with transactional persistence, Viewer protection, profile verification, and fresh PWA delivery for account and sync runtimes.",\n    "Preserves Finance Schema 12, Cloud Schema V3, account balances, paid state, recurrence, project payments, backups, encryption, persistent storage identifiers, and synchronization behavior."',
          )
          query_helper = '''const applyAccountIntegrityAssetQuery = source => source
  .replace(/account-ledger\\.js\\?v=[^\\"'\\s<>)]+/g, `account-ledger.js?v=${ACCOUNT_INTEGRITY_ASSET_QUERY}`)
  .replace(/account-submit-compat\\.js\\?v=[^\\"'\\s<>)]+/g, `account-submit-compat.js?v=${ACCOUNT_INTEGRITY_ASSET_QUERY}`)
  .replace(/cloud-sync\\.js\\?v=[^\\"'\\s<>)]+/g, `cloud-sync.js?v=${ACCOUNT_INTEGRITY_ASSET_QUERY}`)
  .replace(/cloud-sync-lifecycle\\.js\\?v=[^\\"'\\s<>)]+/g, `cloud-sync-lifecycle.js?v=${ACCOUNT_INTEGRITY_ASSET_QUERY}`);

'''
          replace_once(
              "scripts/prepare-runtime.mjs",
              'const normalizeReleaseAssetQuery = source => source.replace(\n  /\\?v=\\d+\\.\\d+\\.\\d+-talaan\\d+/g,\n  `?v=${RELEASE.assetQuery}`\n);\n\n',
              'const normalizeReleaseAssetQuery = source => source.replace(\n  /\\?v=\\d+\\.\\d+\\.\\d+-talaan\\d+/g,\n  `?v=${RELEASE.assetQuery}`\n);\n\n' + query_helper,
          )
          replace_once(
              "scripts/prepare-runtime.mjs",
              '  let next = normalizeReleaseAssetQuery(normalizeRuntimeReferences(source))',
              '  let next = applyAccountIntegrityAssetQuery(normalizeReleaseAssetQuery(normalizeRuntimeReferences(source)))',
          )
          replace_once(
              "scripts/prepare-runtime.mjs",
              'patchTextFile("sw.js", source => normalizeReleaseAssetQuery(normalizeRuntimeReferences(source))',
              'patchTextFile("sw.js", source => applyAccountIntegrityAssetQuery(normalizeReleaseAssetQuery(normalizeRuntimeReferences(source)))',
          )

          worker = read("sw.js")
          worker = worker.replace('asset("./cloud-sync-lifecycle.js?v=2.5.0-talaan1"),', 'asset("./cloud-sync-lifecycle.js?v=2.5.0-account-integrity1"),')
          worker = worker.replace('asset("./cloud-sync.js?v=2.5.0-talaan1"),', 'asset("./cloud-sync.js?v=2.5.0-account-integrity1"),')
          worker = worker.replace('asset("./account-ledger.js?v=2.5.0-talaan1"),', 'asset("./account-ledger.js?v=2.5.0-account-integrity1"),\n  asset("./account-submit-compat.js?v=2.5.0-account-integrity1"),')
          critical_old = 'url.pathname.endsWith("cloud-sync-lifecycle.js") || url.pathname.endsWith("interaction-patterns.js")'
          critical_new = 'url.pathname.endsWith("cloud-sync-lifecycle.js") || url.pathname.endsWith("cloud-sync.js") || url.pathname.endsWith("account-ledger.js") || url.pathname.endsWith("account-submit-compat.js") || url.pathname.endsWith("interaction-patterns.js")'
          if critical_old not in worker:
              raise SystemExit("Expected service-worker critical asset sequence not found")
          worker = worker.replace(critical_old, critical_new, 1)
          write("sw.js", worker)

          replace_once(
              "tests/browser/account-submit-compat.spec.mjs",
              '  await page.waitForFunction(() => Boolean(window.FinanceAccountLedger && window.FinanceAccountSubmitCompat?.installed));',
              '  await page.waitForFunction(() => Boolean(window.FinanceAccountLedger?.capabilities?.accountReconciliationOwner && window.FinanceAccountSubmitCompat?.ledgerGuard));',
          )
          replace_once(
              "tests/browser/account-submit-compat.spec.mjs",
              '  await page.locator("#accountBalance").fill(String(setup.target));',
              '  await page.locator("#accountBalance").fill(setup.target.toLocaleString("en-PH", { minimumFractionDigits:2, maximumFractionDigits:2 }));',
          )

          browser_regression = r'''import { expect, test } from "@playwright/test";
/* global data */

const APP_URL = "http://127.0.0.1:3000";
const ACCOUNT_REFRESH_KEY = "finance-account-integrity-v2-5-0-talaan1";
const STORAGE_KEY = "simple-finance-project-records-v2";
const PROFILE_META_KEY = "simple-finance-profiles-v1";
const PROFILE_DATA_PREFIX = "simple-finance-profile-data-v1:";

test.use({ serviceWorkers:"allow" });

test.beforeEach(async ({ page }) => {
  await page.addInitScript(key => localStorage.setItem(key, "done"), ACCOUNT_REFRESH_KEY);
});

async function authenticate(page) {
  await page.waitForFunction(() => Boolean(window.FinancePrivacyLock));
  await page.waitForFunction(() => !document.body.classList.contains("finance-auth-pending"));
  await page.evaluate(() => window.FinancePrivacyLock.setAuthenticated(true));
  await expect(page.locator("body")).toHaveClass(/finance-signed-in/);
  await page.waitForFunction(() => Boolean(
    window.FinanceAccountLedger?.capabilities?.accountReconciliationOwner
    && window.FinanceAccountSubmitCompat?.ledgerGuard
    && window.FinanceProfileArchitecture
  ));
}

async function openControlledPwa(page, viewport) {
  await page.setViewportSize(viewport);
  await page.goto(`${APP_URL}/?page=money`, { waitUntil:"networkidle" });
  await authenticate(page);
  await page.evaluate(async () => { await navigator.serviceWorker.ready; });
  if (!await page.evaluate(() => Boolean(navigator.serviceWorker.controller))) {
    await page.reload({ waitUntil:"networkidle" });
    await authenticate(page);
  }
  await page.waitForFunction(() => Boolean(navigator.serviceWorker.controller));
}

async function accountSetup(page) {
  return page.evaluate(() => {
    const card = document.querySelector("#moneyAccounts [data-account-card]");
    const account = card?.dataset.accountCard || "";
    const original = Number(data.accounts?.[account] || 0);
    const target = Math.round((original + 12345.67 + Number.EPSILON) * 100) / 100;
    return { account, original, target };
  });
}

async function saveCorrection(page, setup) {
  const card = page.locator(`#moneyAccounts [data-account-card="${setup.account}"]`);
  await card.locator("[data-edit-account]").click();
  await expect(page.locator("#accountDialogTitle")).toHaveText("Edit account");
  await page.locator("#accountBalance").fill(setup.target.toLocaleString("en-PH", { minimumFractionDigits:2, maximumFractionDigits:2 }));
  await page.locator("#accountPrimaryAction").click();
  await expect(page.locator("#accountDialog")).not.toBeVisible();
}

async function readAccountState(page, setup) {
  return page.evaluate(({ account, target }) => {
    const persisted = JSON.parse(localStorage.getItem("simple-finance-project-records-v2") || "{}");
    const profileId = window.FinanceProfileArchitecture?.activeProfileId?.() || "";
    const profile = JSON.parse(localStorage.getItem(`simple-finance-profile-data-v1:${profileId}`) || "{}");
    const reconciliation = (data.accountReconciliations || []).find(item => item.account === account && Number(item.statementBalance) === target) || null;
    const ledgerEntry = reconciliation ? (data.accountLedger || []).find(entry => entry.id === reconciliation.ledgerEntryId) : null;
    return {
      runtime:Number(data.accounts?.[account]),
      persisted:Number(persisted.accounts?.[account]),
      profilePersisted:Number(profile.accounts?.[account]),
      reconciliationId:reconciliation?.id || "",
      ledgerEntryId:ledgerEntry?.id || "",
      ledgerAmount:Number(ledgerEntry?.amount || 0),
      controlled:Boolean(navigator.serviceWorker.controller),
      ledgerAsset:[...performance.getEntriesByType("resource")].some(entry => entry.name.includes("account-ledger.js?v=2.5.0-account-integrity1"))
    };
  }, setup);
}

for (const viewport of [{ name:"desktop", width:1440, height:1000 }, { name:"phone", width:393, height:852 }]) {
  test(`${viewport.name} account correction survives profile persistence and a controlled-PWA reload`, async ({ page }) => {
    await openControlledPwa(page, viewport);
    const setup = await accountSetup(page);
    expect(setup.account).not.toBe("");
    await saveCorrection(page, setup);

    const saved = await readAccountState(page, setup);
    expect(saved.runtime).toBe(setup.target);
    expect(saved.persisted).toBe(setup.target);
    expect(saved.profilePersisted).toBe(setup.target);
    expect(saved.reconciliationId).not.toBe("");
    expect(saved.ledgerEntryId).not.toBe("");
    expect(saved.ledgerAmount).toBeCloseTo(setup.target - setup.original, 2);
    expect(saved.controlled).toBe(true);
    expect(saved.ledgerAsset).toBe(true);

    await page.reload({ waitUntil:"networkidle" });
    await authenticate(page);
    const reloaded = await readAccountState(page, setup);
    expect(reloaded.runtime).toBe(setup.target);
    expect(reloaded.persisted).toBe(setup.target);
    expect(reloaded.profilePersisted).toBe(setup.target);
    expect(reloaded.reconciliationId).toBe(saved.reconciliationId);
    expect(reloaded.ledgerEntryId).toBe(saved.ledgerEntryId);
  });
}

test("Viewer account correction is rejected before ledger or profile mutation", async ({ page }) => {
  await openControlledPwa(page, { width:1440, height:1000 });
  const setup = await accountSetup(page);
  expect(setup.account).not.toBe("");

  await page.evaluate(() => {
    const meta = JSON.parse(localStorage.getItem("simple-finance-profiles-v1") || "{}");
    const active = (meta.profiles || []).find(profile => profile.id === meta.activeProfileId);
    if (!active) throw new Error("Active profile metadata is unavailable");
    active.role = "viewer";
    localStorage.setItem("simple-finance-profiles-v1", JSON.stringify(meta));
  });
  await page.reload({ waitUntil:"networkidle" });
  await authenticate(page);
  await page.waitForFunction(() => window.FinanceProfileArchitecture?.activeRole?.() === "viewer");

  const before = await readAccountState(page, setup);
  const card = page.locator(`#moneyAccounts [data-account-card="${setup.account}"]`);
  await card.locator("[data-edit-account]").click();
  await page.locator("#accountBalance").fill(setup.target.toLocaleString("en-PH", { minimumFractionDigits:2, maximumFractionDigits:2 }));
  await page.locator("#accountPrimaryAction").click();
  await expect(page.locator("#accountDialog")).toBeVisible();

  const after = await readAccountState(page, setup);
  expect(after.runtime).toBe(before.runtime);
  expect(after.persisted).toBe(before.persisted);
  expect(after.profilePersisted).toBe(before.profilePersisted);
  expect(after.reconciliationId).toBe("");
});
'''
          write("tests/browser/account-balance-persistence.spec.mjs", browser_regression)

          sync_regression = r'''#!/usr/bin/env node
import assert from "node:assert/strict";
import fs from "node:fs";
import vm from "node:vm";
import { webcrypto } from "node:crypto";

const source = fs.readFileSync("cloud-sync.js", "utf8");
const profileId = "profile-personal";
const separator = "\u001f";
const account = "Checking";
const original = 466;
const target = 16598;
const difference = target - original;
const openingId = "ledger-opening-v1-Checking";
const reconciliationId = "reconciliation-account-balance-1";
const ledgerId = "ledger-reconciliation-account-balance-1";
const accountKey = `accounts${separator}${account}`;
const openingKey = `accountLedger${separator}${openingId}`;
const ledgerKey = `accountLedger${separator}${ledgerId}`;
const reconciliationKey = `accountReconciliations${separator}${reconciliationId}`;
const baseKey = `simple-finance-cloud-record-base-v3:${profileId}`;
const queueKey = `simple-finance-cloud-record-queue-v3:${profileId}`;
const stores = new Map();

const oldAccountPayload = { name:account, balance:original, type:"Bank", icon:null };
const newAccountPayload = { ...oldAccountPayload, balance:target };
const openingEntry = { id:openingId, operationId:openingId, transactionId:openingId, account, type:"opening-balance", amount:original, date:"2026-09-03", description:"Opening balance" };
const reconciliationEntry = { id:ledgerId, operationId:`reconciliation:${reconciliationId}`, transactionId:reconciliationId, account, type:"reconciliation-adjustment", amount:difference, date:"2026-09-03", description:`Reconciled ${account}`, reconciliationId };
const reconciliation = { id:reconciliationId, account, date:"2026-09-03", previousBalance:original, statementBalance:target, difference, ledgerEntryId:ledgerId };

stores.set(baseKey, JSON.stringify({
  [accountKey]:{ collection:"accounts", recordId:account, payload:oldAccountPayload, sortIndex:0, revision:1, deletedAt:"", updatedAt:"2026-09-03T00:00:00.000Z", updatedByDevice:"cloud-device" },
  [openingKey]:{ collection:"accountLedger", recordId:openingId, payload:openingEntry, sortIndex:0, revision:1, deletedAt:"", updatedAt:"2026-09-03T00:00:00.000Z", updatedByDevice:"cloud-device" }
}));
stores.set(queueKey, JSON.stringify({
  [accountKey]:{ key:accountKey, collection:"accounts", recordId:account, payload:newAccountPayload, sortIndex:0, deleted:false, baseRevision:1, basePayload:oldAccountPayload, baseSortIndex:0, status:"pending", attempts:0, nextAttemptAt:0 },
  [ledgerKey]:{ key:ledgerKey, collection:"accountLedger", recordId:ledgerId, payload:reconciliationEntry, sortIndex:1, deleted:false, baseRevision:0, basePayload:null, baseSortIndex:0, status:"pending", attempts:0, nextAttemptAt:0 },
  [reconciliationKey]:{ key:reconciliationKey, collection:"accountReconciliations", recordId:reconciliationId, payload:reconciliation, sortIndex:0, deleted:false, baseRevision:0, basePayload:null, baseSortIndex:0, status:"pending", attempts:0, nextAttemptAt:0 }
}));

const localStorage = {
  getItem:key => stores.has(key) ? stores.get(key) : null,
  setItem:(key, value) => { stores.set(key, String(value)); },
  removeItem:key => stores.delete(key)
};
const document = {
  readyState:"loading",
  hidden:false,
  addEventListener() {},
  removeEventListener() {},
  getElementById() { return null; },
  querySelector() { return null; }
};
const context = {
  console,
  structuredClone,
  crypto:webcrypto,
  URL,
  TextEncoder,
  TextDecoder,
  setTimeout,
  clearTimeout,
  setInterval,
  clearInterval,
  navigator:{ onLine:false, userAgent:"Node", platform:"Node" },
  document,
  localStorage,
  sessionStorage:{ getItem(){ return null; }, setItem(){}, removeItem(){} },
  matchMedia:() => ({ matches:false, addEventListener(){}, removeEventListener(){} }),
  data:{ accounts:{ [account]:target }, accountTypes:{ [account]:"Bank" }, accountOrder:[account], accountIcons:{}, accountLedger:[openingEntry,reconciliationEntry], accountReconciliations:[reconciliation], ledgerSettings:{ version:1 } },
  normalizeData:value => structuredClone(value),
  STORAGE_KEY:"simple-finance-project-records-v2",
  APP_VERSION:"2.5.0",
  showToast() {},
  location:{ href:"http://127.0.0.1:3000/", origin:"http://127.0.0.1:3000" }
};
context.window = context;
context.globalThis = context;
context.FinanceProfileArchitecture = {
  activeProfileId:() => profileId,
  cloudProfileId:() => "",
  activeRole:() => "owner",
  canWrite:() => true,
  isCloudUnlocked:() => false
};
context.FinanceCloudSyncLifecycle = {
  create:() => ({
    clearForegroundPoll(){}, scheduleForegroundPoll(){}, clearRealtimeRetry(){}, scheduleRealtimeRecovery(){}, noteRealtimeSubscribed(){}
  })
};

vm.createContext(context);
vm.runInContext(source, context, { filename:"cloud-sync.js" });
const internals = context.FinanceCloudSyncInternals;
assert.ok(internals, "Cloud Sync internals were not exposed");

const mapped = internals.toRecordMap(context.data);
assert.equal(mapped[accountKey].payload.balance, target, "corrected account record was not mapped");
assert.equal(mapped[ledgerKey].payload.amount, difference, "reconciliation ledger entry was not mapped");
assert.equal(mapped[reconciliationKey].payload.statementBalance, target, "reconciliation audit record was not mapped");

const overlaps = [];
const merged = internals.threeWayMerge(oldAccountPayload, newAccountPayload, oldAccountPayload, "", overlaps);
assert.equal(merged.balance, target, "an unchanged older cloud account payload overwrote the device correction");
assert.deepEqual(overlaps, [], "unchanged cloud account data should not create an overlap conflict");

internals.applyRemoteEvent({
  collection:"accounts",
  record_id:account,
  payload:oldAccountPayload,
  sort_index:0,
  revision:2,
  deleted_at:"",
  updated_at:"2026-09-03T00:05:00.000Z",
  updated_by_device:"other-device"
});

const pending = JSON.parse(stores.get(queueKey));
assert.equal(pending[accountKey].payload.balance, target, "newer cloud revision discarded the pending corrected account balance");
assert.equal(pending[accountKey].baseRevision, 2, "pending account correction was not rebased onto the newer cloud revision");
assert.equal(pending[accountKey].status, "pending", "rebased account correction is no longer uploadable");
assert.ok(pending[ledgerKey], "reconciliation ledger entry disappeared while rebasing the account record");
assert.ok(pending[reconciliationKey], "reconciliation audit record disappeared while rebasing the account record");

const base = JSON.parse(stores.get(baseKey));
const effective = { ...base };
for (const [key, item] of Object.entries(pending)) {
  effective[key] = {
    collection:item.collection,
    recordId:item.recordId,
    payload:item.payload,
    sortIndex:item.sortIndex,
    revision:item.baseRevision,
    deletedAt:item.deleted ? item.updatedAt || "deleted" : ""
  };
}
const rebuilt = internals.fromRecordStore(effective, context.data);
const ledgerTotal = rebuilt.accountLedger.filter(entry => entry.account === account).reduce((sum, entry) => sum + Number(entry.amount || 0), 0);
assert.equal(rebuilt.accounts[account], target, "effective cloud/device record store reverted the corrected account payload");
assert.equal(ledgerTotal, target, "effective ledger no longer derives the corrected account balance");

console.log("Account reconciliation Cloud Sync regression passed.");
'''
          write("tests/sync/validate-account-reconciliation-sync.mjs", sync_regression)

          replace_once(
              "tests/run.mjs",
              '  { suite: "sync", file: "tests/sync/validate-mobile-cloud-revert-safety.mjs" },',
              '  { suite: "sync", file: "tests/sync/validate-mobile-cloud-revert-safety.mjs" },\n  { suite: "sync", file: "tests/sync/validate-account-reconciliation-sync.mjs" },',
          )
          replace_once(
              "tests/sync/validate-safe-multidevice-sync.mjs",
              'assert(worker.includes(\'asset("./cloud-sync.js?v=2.5.0-talaan1")\'), "PWA shell does not precache the stabilized cloud sync client on the Talaan query");',
              'assert(worker.includes(\'asset("./cloud-sync.js?v=2.5.0-account-integrity1")\'), "PWA shell does not precache the account-integrity cloud sync client");\nassert(worker.includes(\'url.pathname.endsWith("cloud-sync.js")\'), "cloud-sync is no longer delivered network-first");',
          )

          pwa_test = read("tests/regression/validate-pwa-runtime.mjs")
          pwa_test = pwa_test.replace('const phoneFinance = read("phone-finance-compat.js");', 'const phoneFinance = read("phone-finance-compat.js");\nconst accountLedger = read("account-ledger.js");\nconst accountSubmitCompat = read("account-submit-compat.js");')
          pwa_test = pwa_test.replace('const query = "2.5.0-talaan1";', 'const query = "2.5.0-talaan1";\nconst accountIntegrityQuery = "2.5.0-account-integrity1";')
          pwa_test = pwa_test.replace('assert.match(updater, /const INCOME_PLANNING_REFRESH_KEY = "finance-income-planning-v2-5-0-talaan1";/);', 'assert.match(updater, /const INCOME_PLANNING_REFRESH_KEY = "finance-income-planning-v2-5-0-talaan1";/);\nassert.match(updater, /const ACCOUNT_INTEGRITY_REFRESH_KEY = "finance-account-integrity-v2-5-0-talaan1";/);\nassert.match(updater, /async function refreshAccountIntegrityRuntimeOnce\\(\\)/);\nassert.match(updater, /void refreshAccountIntegrityRuntimeOnce\\(\\)\\.then/);')
          marker = 'for (const file of ["budget-planning.js", "productivity-tools.js", "application-help.js", "desktop-ux.css"]) {\n  assert.ok(updater.includes(`"/${file}"`), `${file} must be included in the Income & Planning cache refresh`);\n}\n'
          account_refresh_assertions = marker + 'for (const file of ["account-ledger.js", "account-submit-compat.js", "cloud-sync.js", "cloud-sync-lifecycle.js"]) {\n  assert.ok(updater.includes(`"/${file}"`), `${file} must be included in the Account Integrity cache refresh`);\n}\n'
          if marker not in pwa_test:
              raise SystemExit("Income Planning assertion block not found in validate-pwa-runtime.mjs")
          pwa_test = pwa_test.replace(marker, account_refresh_assertions, 1)
          insertion = '''
assert.ok(index.includes(`./account-ledger.js?v=${accountIntegrityQuery}`), "index must load account-ledger on the Account Integrity asset query");
assert.ok(worker.includes(`./account-ledger.js?v=${accountIntegrityQuery}`), "service worker must precache account-ledger on the Account Integrity asset query");
assert.ok(worker.includes(`./account-submit-compat.js?v=${accountIntegrityQuery}`), "service worker must precache account-submit-compat on the Account Integrity asset query");
assert.ok(worker.includes(`./cloud-sync.js?v=${accountIntegrityQuery}`), "service worker must precache cloud-sync on the Account Integrity asset query");
assert.ok(worker.includes(`./cloud-sync-lifecycle.js?v=${accountIntegrityQuery}`), "service worker must precache cloud-sync-lifecycle on the Account Integrity asset query");
assert.match(worker, /url\\.pathname\\.endsWith\\("account-ledger\\.js"\\)/);
assert.match(worker, /url\\.pathname\\.endsWith\\("account-submit-compat\\.js"\\)/);
assert.match(worker, /url\\.pathname\\.endsWith\\("cloud-sync\\.js"\\)/);
assert.match(accountLedger, /accountReconciliationOwner:true/);
assert.match(accountLedger, /transactionalAccountCorrection:true/);
assert.match(accountLedger, /profileVerifiedAccountCorrection:true/);
assert.match(accountLedger, /function persistAccountMutation\\(/);
assert.match(accountLedger, /if \\(!accountMutationCanWrite\\(\\)\\) return false;/);
assert.match(accountSubmitCompat, /ledgerGuard:true/);
assert.match(accountSubmitCompat, /guardLedgerBackedAccountSubmit/);
'''
          anchor = 'assert.match(worker, /networkFirstCriticalAsset/);'
          if anchor not in pwa_test:
              raise SystemExit("PWA network-first assertion anchor not found")
          pwa_test = pwa_test.replace(anchor, insertion + '\n' + anchor, 1)
          write("tests/regression/validate-pwa-runtime.mjs", pwa_test)

          replace_once(
              "CHANGELOG.md",
              '- Kept the PWA cache at `finance-v2-20260828-household-splits-r17` and advanced the one-time Dashboard presentation refresh so installed clients receive the normalized layouts without changing the product version.',
              '- Kept the PWA cache at `finance-v2-20260828-household-splits-r17` and advanced the one-time Dashboard presentation refresh so installed clients receive the normalized layouts without changing the product version.\n- Hardened Account Ledger balance corrections so edits are reconciled, verified in active-profile storage before the dialog closes, blocked for Viewer profiles, and delivered through fresh network-first account/sync runtime assets without changing Finance Schema 12, Cloud Schema V3, or the product version.',
          )
          PY

          rm .github/workflows/account-integrity-approved.yml

          node --check assets/js/account-ledger.js
          node --check assets/js/account-submit-compat.js
          node --check assets/js/pwa-update.js
          node --check sw.js
          node --check tests/sync/validate-account-reconciliation-sync.mjs

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: preserve account balance corrections"
          git push origin HEAD:main
