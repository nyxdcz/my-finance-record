name: Apply UI Icon Alignment V15.0.4

on:
  push:
    branches:
      - fix/ui-icon-alignment-v15-0-4
    paths:
      - scripts/apply-ui-icon-alignment-v15-0-4.py
      - .github/workflows/apply-ui-icon-alignment-v15-0-4.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Apply UI alignment patch
        run: python3 scripts/apply-ui-icon-alignment-v15-0-4.py

      - name: Preserve executable files
        run: chmod +x Install_V15_0_4.command run_audit.sh

      - name: Install dependencies
        run: npm ci

      - name: Run full repository quality
        run: npm run quality

      - name: Check diff
        run: git diff --check

      - name: Remove one-time patch automation
        run: |
          rm -f scripts/apply-ui-icon-alignment-v15-0-4.py
          rm -f .github/workflows/apply-ui-icon-alignment-v15-0-4.yml

      - name: Commit UI alignment fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "style: tighten V15.0.4 icon alignment"
          git push origin HEAD:fix/ui-icon-alignment-v15-0-4
