name: Apply V15.2.3 release

on:
  push:
    branches:
      - feat/v15-2-3-sync-status-icons

permissions:
  contents: write

jobs:
  apply-release:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v7
        with:
          ref: feat/v15-2-3-sync-status-icons
          fetch-depth: 0
      - name: Use Node.js 24
        uses: actions/setup-node@v7
        with:
          node-version: 24
          package-manager-cache: false
      - name: Apply release changes
        run: node scripts/apply-v15-2-3-release.mjs
      - name: Commit release changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          if git diff --cached --quiet; then
            echo "No release changes to commit."
            exit 0
          fi
          git commit -m "feat: apply V15.2.3 sync status icons"
          git push origin HEAD:feat/v15-2-3-sync-status-icons
