/* Talaan cloud-sync configuration example.
   Copy these values into sync-config.js before deployment.
   Use a Supabase publishable key (sb_publishable_...) or legacy anon key.
   NEVER place a secret key or service_role key in this file. */
window.FINANCE_SYNC_CONFIG = {
  supabaseUrl: "https://YOUR-PROJECT-REF.supabase.co",
  supabasePublishableKey: "sb_publishable_REPLACE_ME"
};
